# Configuration Render pour déploiement automatique
services:
  - type: web
    name: tarot-game
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
