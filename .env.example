# Port du serveur
PORT=3000

# Environnement (development ou production)
NODE_ENV=production

# Nombre maximum de salles simultanées (optionnel)
MAX_ROOMS=100

# Limite de joueurs par salle (optionnel)
MAX_PLAYERS_PER_ROOM=5
